---
const currentYear = new Date().getFullYear();
const socialLinks = [
  { icon: 'fab fa-twitter', url: '#' },
  { icon: 'fab fa-instagram', url: '#' },
  { icon: 'fab fa-facebook', url: '#' },
  { icon: 'fab fa-pinterest', url: '#' }
];
---

<footer class="bg-[#c0392b] text-white py-8 border-t-4 border-[#2e7d32]">
  <div class="container mx-auto px-4 text-center">
    <p class="text-lg font-serif mb-4">&copy; {currentYear} Porky Pie Chronicles. All rights reserved.</p>
    
    <div class="flex justify-center gap-6 mb-6">
      {socialLinks.map((link) => (
        <a href={link.url} class="hover:text-[#2e7d32] transition-colors duration-300 transform hover:scale-110">
          <i class={link.icon + " text-2xl"}></i>
        </a>
      ))}
    </div>
    
    <div class="border-t border-white pt-4">
      <p class="text-sm opacity-80">Crafted with vintage charm and modern precision</p>
    </div>
  </div>
</footer>